import {MAT_FORM_FIELD_PROVIDER} from './mat-form-field.provider';
import {MAT_DIALOG_PROVIDER} from './mat-dialog.provider';
import { HTTP_TOKEN_INTERCEPTOR_PROVIDER } from './http-token.provider';
import { HTTP_ERROR_INTERCEPTOR_PROVIDER } from './http-error.provider';

export {
  MAT_FORM_FIELD_PROVIDER,
  MAT_DIALOG_PROVIDER,
  HTTP_TOKEN_INTERCEPTOR_PROVIDER,
  HTTP_ERROR_INTERCEPTOR_PROVIDER
}
